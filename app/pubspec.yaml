name: fundus
description: Local-first media library and playback client.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.11.1

resolution: workspace

dependencies:
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  fundus_core: any
  fundus_server: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
